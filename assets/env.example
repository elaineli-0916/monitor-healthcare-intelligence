# Copy to .env in the runtime directory. Fill only the adapters you use.

EMAIL_WEBHOOK_URL=
EMAIL_WEBHOOK_TOKEN=

SMTP_HOST=smtp.gmail.com
SMTP_USERNAME=
SMTP_APP_PASSWORD=
MAIL_FROM=

LLM_BASE_URL=
LLM_API_KEY=
LLM_MODEL=

# Optional. Reference these as ${WERSS_BASE_URL} and ${WERSS_ACCESS_KEY}
# inside a critical source row's WeRSS URL.
WERSS_BASE_URL=
WERSS_ACCESS_KEY=
